{
  "name": "codenamesti",
  "version": "0.0.1",
  "scripts": {
    "build": "cd apps/frontend && npm run build",
    "start": "cd apps/backend && npm start"
  },
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.0",
    "prettier": "^1.16.4"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
